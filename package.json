{
  "name": "axt-budget",
  "version": "0.1.0",
  "private": true,
  "description": "AXT 예산 집행 자동화 — Google Sheets + Apps Script (clasp)",
  "scripts": {
    "login": "clasp login",
    "push": "clasp push",
    "pull": "clasp pull",
    "open": "clasp open"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
